This directory holds third-party transit datasets downloaded at runtime
(routes.json, stops.json, metadata.json).

Those files are not part of the AGPL-3.0 project license. They are cached
locally so OpenBahía can work offline after a successful fetch. Do not
redistribute them unless the original publisher grants that right.
